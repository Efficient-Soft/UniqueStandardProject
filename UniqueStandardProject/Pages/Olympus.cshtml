@page
@model UniqueStandardProject.Pages.OlympusModel
@{
    ViewData["Title"] = "Home Page";
}

<!-- Service Start -->
<div class="container-xxl py-5">
    <div class="container">
        <div class="text-center wow fadeInUp" data-wow-delay="0.1s">
            <h1 class="mb-5">Olympus</h1>
        </div>
        <div class="row text-center wow fadeInUp" id="olympus">
        </div>
    </div>
</div>
<!-- Service End -->

<script>
    var url = 'api/products/productDetail?productId=3';

    fetch(url)
        .then((res) => res.json())
        .then((response) => {
            console.log(response);
            if (response.success) {
                if (Array.isArray(response.data)) {
                    generateHtml(response.data);
                } else {
                    console.error('The data received is not an array.');
                }
            } else {
                console.error('Request was not successful.');
            }
        })
        .catch((error) => {
            console.error('Error fetching data:', error);
        });

    const generateHtml = (data) => {
        var olympus = document.getElementById('olympus');
        var html = ``;
        data.forEach((player) => {
            html +=
                `<div class="col-md-4">
                    <img src="${player.img}" style="width: 100%; height: 300px; object-fit: cover;">
                    <p class="name" style="color:primary; font-size: 20px;width: 100%;"><center>${player.title}</center></p>
                </div>`;
        });

        if (olympus) {
            olympus.innerHTML = html;
        } else {
            console.error("Element not found or not loaded yet.");
        }
    };

</script>